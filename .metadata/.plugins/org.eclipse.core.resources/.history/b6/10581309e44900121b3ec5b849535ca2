package com.dbraillon.pocspotifymobile
{
	public class Log
	{
		public static function write(level:int, context:String, message:String):void
		{
			var localDate:Date = new Date();
			trace("(" + Date. + " - " + level + ")[" + context + "]: " + message);
		}
	}
}